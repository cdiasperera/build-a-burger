const initialState = {
  ingredients: null,
  price: 0
}

const orderReducer = (prevState = initialState, action) => {
  switch (action.type) {
    default:
      return prevState
  }
}

export default orderReducer
